// API Configuration
export const API_BASE_URL = 'http://localhost:5007'; // Update this with your backend URL

// Local Storage Keys
export const TOKEN_KEY = 'token';
export const USER_KEY = 'user';

// Default Pagination
export const DEFAULT_PAGE_SIZE = 10;

export default {
  API_BASE_URL,
  TOKEN_KEY,
  USER_KEY,
  DEFAULT_PAGE_SIZE
};
